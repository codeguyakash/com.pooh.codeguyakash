import { StyleSheet } from 'react-native';

export const globalStyle = StyleSheet.create({
  safe: {
    flex: 1,
  },
  input: {
    height: 48,
    borderWidth: 1,
    borderRadius: 8,
    paddingHorizontal: 15,
    marginBottom: 15,
    fontSize: 16,
  },

  button: {
    paddingVertical: 14,
    borderRadius: 8,
    alignItems: 'center',
  },
});
